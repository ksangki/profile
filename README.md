# 김상기 — Sangki Kim

SK텔레콤 Developer Relations 포트폴리오. 한국어 `/`, English `/en`.

## 로컬

```bash
npm install
npm run dev
```

## Vercel

이 저장소를 [Vercel](https://vercel.com/new)에서 Import 하면 됩니다.

- Framework: Other (Nitro가 `.vercel/output`을 만듭니다)
- Build Command: `npm run build`
- Install Command: `npm install`

배포 후 `https://<project>.vercel.app` 주소가 생기고, 영문은 `/en`입니다.
